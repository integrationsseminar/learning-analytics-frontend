import 'package:flutter/material.dart';

class MeinProfilS extends StatelessWidget {
  const MeinProfilS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('MeinProfilS'),
    );
  }
}
