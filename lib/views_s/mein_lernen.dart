import 'package:flutter/material.dart';

class MeinLernenS extends StatelessWidget {
  const MeinLernenS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('MeinLernenS'),
    );
  }
}
