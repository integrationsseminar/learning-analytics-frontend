import 'package:flutter/material.dart';

class MeinLernenD extends StatelessWidget {
  const MeinLernenD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('NTH but we\'ll see'),
    );
  }
}
