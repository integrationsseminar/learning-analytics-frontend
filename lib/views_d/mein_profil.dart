import 'package:flutter/material.dart';

class MeinProfilD extends StatelessWidget {
  const MeinProfilD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('MeinProfilD'),
    );
  }
}
