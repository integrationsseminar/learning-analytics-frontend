import 'package:flutter/material.dart';

class MeineKurseD extends StatelessWidget {
  const MeineKurseD({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('NTH but we\'ll see'),
    );
  }
}
